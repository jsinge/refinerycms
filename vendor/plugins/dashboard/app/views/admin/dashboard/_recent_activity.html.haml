#recent_activity
  %h2= t('.latest_activity').titleize
  %ul.clickable
    - @recent_activity.each do |change|
      - begin
        %li{ :class=> cycle("on", "on-hover", :name => 'recent_activity_cycle')}
          = activity_message_for change
      - rescue
        -
