%li
  %h3= t('product.shop')
  = link_to shop.title, shop
  .vendor
    = t('product.vendor')
    %br
    = image_fu shop.user.image, :preview
    = shop.user.name
  %h3= t('more_products')
  = render shop.products.randomize.limit(3).without(@product)
